.DS_Store

# context-sync harvest: transient read-only clones + regenerated triage files.
# The dedup ledger (inbox/.harvested) is committed; per-run reports are not.
/.harvest-cache/
/inbox/harvest-*.md
